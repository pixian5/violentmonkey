# 2026-08-05 代码审查修复

- CI 改为在测试前使用锁定文件安装依赖，并允许 Electron 安装脚本执行。
- 本地化构建只枚举语言目录，避免 macOS `.DS_Store` 阻断构建。
- 修复 GM_download 的下载错误回调、URL 校验和终结事件清理。
- 修复 ZIP 导入日期、重复端口监听器和导入后索引缓存重建。
- OAuth 回调仅接受当前授权标签页，并在创建标签页前注册监听器。
- Electron webview 弹窗事件阻止默认窗口创建，避免重复打开窗口。
